{
  "name": "@rohinik/config",
  "version": "0.0.1",
  "private": true,
  "description": "Shared site configuration — navigation, footer, site metadata",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
